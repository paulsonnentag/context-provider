import type { Example } from "../types";
import { SpatialCanvasExample } from "./setup";

const example: Example = {
  name: "Spatial Canvas",
  description: "Canvas doc with draggable shapes embedding Counter docs.",
  component: SpatialCanvasExample,
};

export default example;
